import styled from 'styled-components';

const ViewerAuthorBookTitle = styled.figcaption`
  text-align: justify;
  margin-bottom: 0.5rem;
  color: var(--color-text);
`;

export default ViewerAuthorBookTitle;
